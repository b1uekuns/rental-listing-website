<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> | Quản trị Góc Thuê Nhà</title>
    <!-- Reset CSS -->
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html,
      body {
        height: 100%;
        margin: 0;
      }
    </style>
    <link rel="stylesheet" href="/css/admin.css" />
  </head>
  <body>
    <header class="admin-header">
      <a href="/admin" class="logo">Góc Thuê Nhà - Admin</a>
      <div class="admin-info">
        <span>Xin chào, <%= user ? user.full_name : 'Admin' %></span>
        <img
          src="<%= user && user.avatar_url ? user.avatar_url : '/images/default-avatar.png' %>"
          alt="avatar"
          class="admin-avatar"
        />
        <a href="/auth/logout" class="btn">Đăng xuất</a>
      </div>
    </header>
